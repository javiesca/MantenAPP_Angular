import { PressEffectDirective } from './press-effect.directive';

describe('PressEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new PressEffectDirective();
    expect(directive).toBeTruthy();
  });
});
